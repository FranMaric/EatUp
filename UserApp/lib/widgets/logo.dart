import 'package:flutter/material.dart';

class EatUpLogo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text(
      'EatUp',
      style: TextStyle(
        color: Colors.white,
        fontWeight: FontWeight.w600,
        fontSize: 24,
      ),
    );
  }
}
